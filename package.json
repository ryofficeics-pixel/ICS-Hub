{
  "name": "ics-tools-hub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "check:env": "node scripts/check-env.js",
    "check:docs": "node scripts/check-doc-authority.js",
    "check:portable": "node scripts/check-portability.js",
    "bootstrap:first-admin": "node scripts/bootstrap-first-admin.js",
    "build": "npm run check:env && vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.106.1"
  }
}
